// Alert dialog
/*
 * Alert dialog
 */
import alertdialog.AlertDialog;

$SET_MESSAGE = setMessage(String message);

$SET_TITLE = setTitle(String title);

$BUILD = $SET_TITLE $SET_MESSAGE;

$SHOW = show();

AlertDialog {
  void $BUILD $SHOW;
}
